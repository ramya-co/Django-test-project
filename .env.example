# ── GitHub integration — Sentry crash-triage pipeline ──────────────────────
# Copy this file to .env (which is git-ignored) and fill in real values.
# Never commit the real .env file to version control.

# A GitHub Personal Access Token with the following scopes on the target repo:
#   contents (read/write), issues (read/write), pull-requests (read/write),
#   workflows (read/write)
GH_PAT=github_pat_your_token_here

# The GitHub account or organisation that owns the target repository
GH_OWNER=your-github-username

# The repository name (without the owner prefix)
GH_REPO=your-repo-name
